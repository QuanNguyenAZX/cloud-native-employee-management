backend:
  image:
    repository: backend
    tag: latest
    pullPolicy: IfNotPresent
  frontendHost: http://employee.local
  corsOrigins: http://employee.local

frontend:
  image:
    repository: frontend
    tag: latest
    pullPolicy: IfNotPresent
  apiBaseUrl: /api/v1

ingress:
  host: employee.local

persistence:
  postgres:
    size: 10Gi
  minio:
    size: 5Gi
