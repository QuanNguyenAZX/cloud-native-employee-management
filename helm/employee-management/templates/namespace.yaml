{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "employee-management.namespace" . }}
  labels:
    {{- include "employee-management.labels" . | nindent 4 }}
{{- end }}
